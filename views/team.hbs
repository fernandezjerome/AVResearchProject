<section>

    <h1>{{person.name}}</h1>
    <h2> {{person.role}}</h2>
    <p> Team members name</p>

    <!-- a bio image or something-->
    <img src="/images/{{person.image}}" alt="{{person.name}}'s avatar" />
</section>